type Mutation {
    # Auth

    """Sign in user with username and password"""
    login(username: String!, password: String!): AuthPayload

    # User

    """Sign up user with UserInput"""
    userCreate(input: UserInput!): User

    """Update currently authenticated user with UserUpdateInput"""
    userUpdate(input: UserUpdateInput!): User

    """Delete currently authenticated user"""
    userDelete: User

    # File

    """Create file with FileInput"""
    fileCreate(input: FileInput!): File

    """Update file with id and FileUpdateInput"""
    fileUpdate(id: ID!, input: FileUpdateInput!): File

    """Delete file with id"""
    fileDelete(id: ID!): File

    # File share

    """Create file share with FileShareInput"""
    fileShareCreate(input: FileShareInput!): FileShare

    """Update file share with id and FileShareUpdateInput"""
    fileShareUpdate(id: ID!, input: FileShareUpdateInput!): FileShare

    """Delete file share with id"""
    fileShareDelete(id: ID!): FileShare

    # File tag

    """Delete file tag with id"""
    fileTagDelete(fileId: ID!, tagId: ID!): FileTag

    # Tag

    """Create tag with TagInput"""
    tagCreate(input: TagInput!): Tag

    """Update tag with id and TagUpdateInput"""
    tagUpdate(id: ID!, input: TagUpdateInput!): Tag

    """Delete tag with id"""
    tagDelete(id: ID!): Tag

    # Tag share

    """Create tag share with TagShareInput"""
    tagShareCreate(input: TagShareInput!): TagShare

    """Update tag share with id and TagShareUpdateInput"""
    tagShareUpdate(id: ID!, input: TagShareUpdateInput!): TagShare

    """Delete tag share with id"""
    tagShareDelete(id: ID!): TagShare
}
