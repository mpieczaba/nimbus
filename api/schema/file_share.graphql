type FileShare {
    id: ID!
    file: File!
    user: User!
    permissions: Int!
}

input FileShareInput {
    fileId: ID!
    userId: ID!
    permissions: Int!
}

input FileShareUpdateInput {
    permissions: ID!
}
