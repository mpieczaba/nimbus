type FileShare {
    user: User!
    permissions: FileSharePermissions!
    createdAt: Time!
    updatedAt: Time!
}

input FileShareInput {
    userId: ID!
    permissions: FileSharePermissions!
}

enum FileSharePermissions {
    CoOwner
    Editor
    Viewer
}
