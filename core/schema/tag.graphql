type Tag {
    id: ID!
    name: String!
    owner: User!
    sharedFor: [TagShare!]!
    createdAt: Time!
    updatedAt: Time!
}

type TagShare {
    user: User!
    permissions: Int!
}

input TagInput {
    name: String!
}

input TagUpdateInput {
    name: String
    ownerId: ID
}
